1. supporse x and y are random variables with joint PDF f_xy(x, y) = 1/8 if abs(x) + abs(y) <= 2 otherwise 0.
note that the region where this PDF is non-zero is the union of four triangles. one in each quardant.
Denote A= {y >= x^2}
a): calculate the P(A)
b): find f_y(y|A)
c): computer var(x|A)

2. RVs x and y are jointly normal. Both have mean 1 ans variance 2, and their covariance is -1
a): write the explicit expression for f_xy(x, y)
b): sketch the covariance ellipse of [x, y] transpose
c): find the orthogonal(real unitary) matrix U so that the random vector W = U[x, y]^T uncorrlated
d) compute E[W]

3. z is a complex random 3-vector with mean E[z] and covariance matrix C_z as follows:
E[z] = [0, -i 1]^T and C_z = [[4, i, a], [b, 3, 0], [1, c, 3]]
a) find the values of a, b and c
b) if x = 2z1 - 3z2 + (2i + 1)z3, calculate var(x)

4. RVs x1,...,xn are iid and uniformly distributed on [0, theta] with theta > 0, consider and estimator thetabar of the parameter
theta defined by thetabar = 2/n(sigma(x_k), k from 1 to n)
calculate the bias beta(thetabar) and the variance var(thetabar) of the estimator

5. an estimator thetabar of a parameter theta and bias bata. A new estimator of theta is defined as theta1bar = 2thetabar. what's
bias of theta1bar


###############


1. the joint pdf of RVs x and y is f_xy(x, y) = 2/3 if -1<=x<=0, 0<=y<=1; 2/3 if 0<=x <=1, 0<=y<=1, x+y <=1; 0 otherwise
define events A={x <0} and B = {x >0}

a):calculate E[x] and E[y]
b): find E[x|A] and E[y|B]
c): are x and y independent? are they independent given A, are they independent given B?


2. A real random 2-vector X=[x1, x2]^T is normal with mean [1, -1]^T adn covariance matrix C_x = [[10, -2*sqrt(3)], [-2*sqrt(3), 6]]
a): write an expression for teh PDF f_x2 of the RV x_2
b):sketch the covariance ellipse of X
c): find the unitary matrix U so that Y=UX has a digonal covariance matrix C_Y
d):sketch the covariance ellipse of Y

3. Z is a complex random 3-vector with mean M = [i, -2, 0]^T and covariance matrix C_z as follows:
C_z = [[3, 1, i], [1, 3, 0], [-i, 0, 3]]
 define w1=2iz1-z3 w2=z2+(1+i)z3 calculate E[abs(w2)^2]

4. RVs x1...xn are iid are uniformly distributed on [0, theta] with theta >0 consider an estimator thetabar of theta defined by thetabar = max(x1, .... xn)
calculate the bias beta(thetabar) and the variance var(thetabar) of this estimator

5. thetabar and thetawave are two estimators of a parameter theta that use data x1... xn. their respective bias are beta(thetabar) = 0.1theta-2
beta(thetawave) = -0.2theta + 1 find a,b so that estimator thetabar = a*thetabar + b*thetawave is unbiased



####
2. The random variable x has the truncated exponential
density f (x) = ce^(−c(x−x0))*U(x − x0). Find the ML estimate c cap of c in terms of the n samples xi of x

a complex discrete-time WSS process z(·) has mean
zero and spectral density Sz. A new process w(·) is defined by w(k) = z(k)eiθ
where θ ~ U [0, 2PI] is independent of z(·). Show that Sw(omega) = Sz(omega) for all omega.

A discrete-time white process x(·) with variance sigma^2 is passed through a stable discrete-time LTI system
with impulse response h(·). Find the variance of the output process y(·) in terms of sigma^2 and h.

A zero-mean WSS discrete-time process y(·) has spectral density
S_y(omega) = (5 + 3 cos(omega)) / (17 + 8 cos(omega))
(a) Find the frequency response H(omega) of a discrete-time LTI system that will produce a process with
this spectral density from a unit-variance white input signal x(·).
(b) Calculate the autocovariance function c_y(k) for κ>=0.
(c) Find the impulse response of a whitening filter for y(·).


7. With theta ~ U[0,2π), show that the continuous-time process x(t) = cos(t+ theta) is WSS. Find the spectral
density S_x in terms of the Dirac delta symbol.

8. Independent RVs ξ and η both have mean zero and variance σ2. Find the autocovariance function of
the continuous-time process
x(t) = ξ cos(ωt) + η sin(ωt)
for fixed ω. [Hint: There are only two degrees of freedom in x(·).]

8. independent RVs xi and eta both have mean zero and variance sigma^2. find the autocovariance function of the
continuous-time process x(t) = xi*cos(omega*t) + eta*sin(omega*t) for fixed omega.

9. A continuous-time process x(·) is WSS with mean zero and spectral density S_x(omega) = a2 − omega^2 if |omega|<=a
S_x(omega) = 0 if |omega| >a
Calculate var(x(t)).

10. The spectral density of a continuous-time WSS process x(·) is Sx(ω) = 1− |ω| W |ω|≤W 0 |ω|>W What is the autocovariance function of this process?
10. The spectral density of a continuous-time WSS process x(·) is S_x(omega) = 1− |omega| / W if |omega|≤W,  0 if |omega|>W What is the autocovariance function of this process?

11. A continuous-time stochastic process x(·) is said to have independent increments if, for all t1 < t2 <
t3 < t4, x(t4)−x(t3) is independent of x(t2)−x(t1). For such a process, determine cx(t1,t2) for
0 <= t1 <t2 in terms of var(x(t1)) assuming x(0) = 0.

1. a)
a discrete-time stochastic process x(·) is defined by x(k) = u_k for k even and x(k) = v_k for k odd, where u_k are iid and ~U[0, 1] and v_k are
iid ~ N[0, 1]. Are the processes x(·) WSS?
b) define a new discrete-time process y(·) by y(k) = x(-4k + 3). Find the mean and autocovariance function of y(·). verify y(·) is WSS
c) define a new process z(·) by z(k) = -y(k - 2) + 2y(k - 1) + 2y(k + 1) - y(k+2). Find the mean and autocovariance function of z(·). calulate and plot a sketch of the spectral density
S_z(omega).



2 a) RVs x1 ... xn are independent and laplace distributed with standard deviation sigma. i.e. their PDF is f(x) = sqrt(2)/(2*sigma)*exp(-sqrt(2)*|x|/sigma).
Find the ML estimate for the parameter sigma
b) a single measurement x is laplace distributed under both H0 and H1 with sigma0 < sigma1. the prior probability of H1 is P(H1) = 0.4 for what values of x will
the bayesian decision rule decide in favor of


